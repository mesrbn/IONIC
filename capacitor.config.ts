import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'downloadFile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
